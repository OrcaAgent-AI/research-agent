# Required API Keys
TAVILY_API_KEY="your_tavily_api_key_here"

# LangSmith Tracing (Optional)
LANGSMITH_PROJECT=research-agent
LANGSMITH_API_KEY="your_langsmith_api_key_here"
LANGSMITH_TRACING="true"
LANGCHAIN_TRACING_V2="true"

## LLM Configuration (Global Defaults)
# OpenAI (or OpenAI-compatible API)
DEFAULT_API_KEY=
DEFAULT_BASE_URL=
DEFAULT_MODEL_NAME=
DEFAULT_MODEL_PROVIDER=


## Agent Model Configuration (Optional)
# Specify different models for different agent tasks
# If not set, will use MODEL_NAME, MODEL_PROVIDER, OPENAI_API_KEY, OPENAI_BASE_URL as defaults

# Query Generator Configuration
# QUERY_GENERATOR_MODEL="gpt-4o-mini"  # Model for generating search queries
# QUERY_GENERATOR_PROVIDER="openai"    # Provider: openai|anthropic|google-genai
# QUERY_GENERATOR_API_KEY="your_api_key"  # Defaults to OPENAI_API_KEY if not set
# QUERY_GENERATOR_BASE_URL="https://api.openai.com/v1"  # Defaults to OPENAI_BASE_URL if not set

# Reflection Model Configuration
# REFLECTION_MODEL="gpt-4o-mini"       # Model for reflection and knowledge gap analysis
# REFLECTION_PROVIDER="openai"         # Provider: openai|anthropic|google-genai
# REFLECTION_API_KEY="your_api_key"    # Defaults to OPENAI_API_KEY if not set
# REFLECTION_BASE_URL="https://api.openai.com/v1"  # Defaults to OPENAI_BASE_URL if not set

# Answer Model Configuration
# ANSWER_MODEL="gpt-4o"                # Model for final answer generation
# ANSWER_PROVIDER="openai"             # Provider: openai|anthropic|google-genai
# ANSWER_API_KEY="your_api_key"        # Defaults to OPENAI_API_KEY if not set
# ANSWER_BASE_URL="https://api.openai.com/v1"  # Defaults to OPENAI_BASE_URL if not set

## Agent Behavior Configuration (Optional)
# NUMBER_OF_INITIAL_QUERIES=3  # Number of initial search queries to generate
# MAX_RESEARCH_LOOPS=2         # Maximum number of research loops to perform